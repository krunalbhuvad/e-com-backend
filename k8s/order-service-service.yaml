apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ops
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 4003
      targetPort: http
